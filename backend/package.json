
{
  "name": "neighborhood-portal-backend",
  "version": "1.0.0",
  "description": "Backend API for Neighborhood Portal",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx ts-node-dev --respawn --transpile-only src/server.ts",
    "migrate": "mysql -u root -p < schema.sql"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.5",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "jsonwebtoken": "^9.0.2",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "typescript": "^5.3.3",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.4"
  }
}
